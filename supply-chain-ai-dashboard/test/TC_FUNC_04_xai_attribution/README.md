# TC-FUNC-04：XAI 局部特征归因归一化测试

### 1. 测试流程
1. 向流式数据注入接口发送会触发拦截的亏损订单。
2. 校验返回体中 `alert.xai_analysis` 指标：
   *   权重之和应为 `1.0`（通过百分比归一化呈现）。
   *   对于财务亏损订单，订单利润贡献度应高居榜首。
3. 执行 `test_xai.py` 脚本：
   ```bash
   python test/TC_FUNC_04_xai_attribution/test_xai.py
   ```

### 2. 测试结果
*   **状态**：**PASS**
*   **实际日志**：
    ```text
    === 开始执行 TC-FUNC-04 (XAI 局部特征归因归一化测试) ===
    响应状态码: 200
    提取的 XAI 归因字典: {"订单利润": 0.754, "订单总额": 0.195, "运输模式": 0.051}
    XAI 权重总和: 1.0
    -> [PASS] XAI 可解释性分析模型输出 of 特征归因格式及数学归一化校验全部通过！
    ```
