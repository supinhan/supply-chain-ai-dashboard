# TC-PERF-02：高吞吐并发压力测试

### 1. 测试流程
1. 使用 Python 的 `concurrent.futures.ThreadPoolExecutor` 模拟 8 线程并发调用。
2. 总共发起 40 次请求（包括订单流数据注入与时序销量预测）。
3. 统计接口请求成功率及每秒请求数 (QPS)。
4. 执行 `test_load.py` 脚本：
   ```bash
   python test/TC_PERF_02_load/test_load.py
   ```

### 2. 测试结果
*   **状态**：**PASS**
*   **实际日志**：
    ```text
    === 开始执行 TC-PERF-02 (高吞吐并发压力测试) ===
    模拟并发线程数: 8, 总调用请求: 40 次...

    --- 并发测试结果 ---
    总耗时: 1.52 s
    成功次数: 40 / 40
    请求成功率: 100.0%
    吞吐量 (QPS): 26.3 req/sec
    -> [PASS] 高并发压力测试通过，多线程下系统仍能稳定处理请求！
    ```
