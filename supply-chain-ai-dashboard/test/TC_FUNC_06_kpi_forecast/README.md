# TC-FUNC-06：历史趋势与销量预测查询测试

### 1. 测试流程
1. 请求历史 trends 接口 `GET /api/v1/kpi/history?hours=24`，验证返回结构体中包含聚合好的时间槽 `bucket` 和基础项数据（例如 `order_count`, `risk_count`）。
2. 请求未来预测接口 `GET /api/v1/forecast`，验证输出中包含对齐的 `days` 数组（未来 7 天）以及预测数值 `values` 数组。
3. 执行 `test_kpi.py` 脚本：
   ```bash
   python test/TC_FUNC_06_kpi_forecast/test_kpi.py
   ```

### 2. 测试结果
*   **状态**：**PASS**
*   **实际日志**：
    ```text
    === 开始执行 TC-FUNC-06 (历史趋势与销量预测查询测试) ===

    [步骤 1] 调取历史24小时 KPI 聚合趋势接口...
    历史趋势状态码: 200
    返回条数: 1
    首项结构: {'bucket': '2026-07-02T14:00:00', 'order_count': 4, 'risk_count': 2, 'gmv': 1070.5}
    -> [PASS] 历史 KPI 聚合接口校验成功！

    [步骤 2] 调取未来7天时序销量预测接口...
    销量预测状态码: 200
    预测天数序列 (days): ['第 1 天', '第 2 天', '第 3 天', '第 4 天', '第 5 天', '第 6 天', '第 7 天']
    对应销量值序列 (values): [1.5, 1.5, 1.49, 1.5, 1.5, 1.5, 1.5]
    -> [PASS] 未来7天时序销量预测接口校验成功！
    ```
